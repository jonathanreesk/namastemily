{
  "name": "namaste-emily",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "openai": "^4.67.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
